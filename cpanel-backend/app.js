// cPanel "Application startup file" — point cPanel here.
// Loads the bundled server. No npm install required: everything is
// already inlined inside server.cjs (3.6 MB, pure JavaScript).
"use strict";
require("./server.cjs");
